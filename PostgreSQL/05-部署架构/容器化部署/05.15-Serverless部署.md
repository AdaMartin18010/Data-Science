# Serverless 部署指南

> **文档版本**: v1.0
> **最后更新**: 2025-11-13
> **版本覆盖**: PostgreSQL 18.x (推荐) ⭐ | 17.x (推荐) | 16.x (兼容)
> **文档状态**: ✅ 已创建

---

## 📋 目录

- [Serverless 部署指南](#serverless-部署指南)
  - [📋 目录](#-目录)
  - [1. 概述](#1-概述)
    - [主要 Serverless 方案](#主要-serverless-方案)
  - [2. Neon Serverless PostgreSQL](#2-neon-serverless-postgresql)
    - [2.1 概述](#21-概述)
    - [2.2 快速开始](#22-快速开始)
      - [创建项目](#创建项目)
      - [连接数据库](#连接数据库)
    - [2.3 分支功能](#23-分支功能)
      - [创建分支](#创建分支)
      - [使用分支](#使用分支)
    - [2.4 配置优化](#24-配置优化)
      - [连接池配置](#连接池配置)
      - [性能优化](#性能优化)
  - [3. Supabase](#3-supabase)
    - [3.1 概述](#31-概述)
    - [3.2 快速开始](#32-快速开始)
      - [创建项目](#创建项目-1)
      - [连接数据库](#连接数据库-1)
    - [3.3 实时功能](#33-实时功能)
    - [3.4 存储集成](#34-存储集成)
  - [4. AWS RDS Serverless v2](#4-aws-rds-serverless-v2)
    - [4.1 概述](#41-概述)
    - [4.2 创建 Serverless 集群](#42-创建-serverless-集群)
      - [使用 AWS CLI](#使用-aws-cli)
      - [使用 Terraform](#使用-terraform)
    - [4.3 配置自动扩缩容](#43-配置自动扩缩容)
  - [5. Azure Database for PostgreSQL Flexible Server](#5-azure-database-for-postgresql-flexible-server)
    - [5.1 概述](#51-概述)
    - [5.2 创建 Flexible Server](#52-创建-flexible-server)
      - [使用 Azure CLI](#使用-azure-cli)
      - [使用 Azure Portal](#使用-azure-portal)
    - [5.3 配置自动扩缩容](#53-配置自动扩缩容)
  - [6. 分支与开发工作流](#6-分支与开发工作流)
    - [6.1 Neon 分支工作流](#61-neon-分支工作流)
    - [6.2 Supabase 分支工作流](#62-supabase-分支工作流)
    - [6.3 最佳实践](#63-最佳实践)
  - [7. 成本优化](#7-成本优化)
    - [7.1 Neon 成本优化](#71-neon-成本优化)
    - [7.2 AWS RDS Serverless 成本优化](#72-aws-rds-serverless-成本优化)
    - [7.3 通用优化建议](#73-通用优化建议)
  - [8. 迁移到 Serverless](#8-迁移到-serverless)
    - [8.1 从自建 PostgreSQL 迁移](#81-从自建-postgresql-迁移)
      - [使用 pg\_dump](#使用-pg_dump)
      - [使用逻辑复制](#使用逻辑复制)
    - [8.2 迁移检查清单](#82-迁移检查清单)
  - [9. 深入阅读](#9-深入阅读)
    - [相关文档](#相关文档)
    - [外部资源](#外部资源)

---

## 1. 概述

Serverless PostgreSQL 提供按需扩展、自动扩缩容、按使用量计费的数据库服务，特别适合：

- **开发测试环境**: 快速启动，按需付费
- **中小型应用**: 自动扩缩容，降低运维成本
- **突发流量场景**: 自动应对流量峰值
- **多环境管理**: 分支功能支持快速创建测试环境

### 主要 Serverless 方案

| 方案 | 特点 | 适用场景 |
|------|------|---------|
| **Neon** | 分支功能、冷启动快、按需计费 | 开发测试、多环境 |
| **Supabase** | 全栈平台、实时功能、存储集成 | 全栈应用、实时应用 |
| **AWS RDS Serverless v2** | 企业级、高可用、自动扩缩容 | 生产环境、企业应用 |
| **Azure Flexible Server** | 企业级、高可用、混合部署 | 企业应用、混合云 |

---

## 2. Neon Serverless PostgreSQL

### 2.1 概述

Neon 是专为 Serverless 设计的 PostgreSQL 服务，提供：

- ✅ **分支功能**: 快速创建数据库分支
- ✅ **冷启动优化**: 秒级启动
- ✅ **按需计费**: 按计算和存储使用量计费
- ✅ **自动扩缩容**: 根据负载自动调整
- ✅ **PostgreSQL 18 支持**: 最新版本支持

### 2.2 快速开始

#### 创建项目

```bash
# 安装 Neon CLI
npm install -g neonctl

# 登录
neonctl auth

# 创建项目
neonctl projects create --name myproject

# 获取连接字符串
neonctl connection-string --project-id <project-id>
```

#### 连接数据库

```bash
# 使用连接字符串
psql "postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# 或使用环境变量
export DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
psql $DATABASE_URL
```

### 2.3 分支功能

#### 创建分支

```bash
# 创建开发分支
neonctl branches create --project-id <project-id> --name dev

# 创建功能分支
neonctl branches create --project-id <project-id> --name feature-auth --parent-id <parent-branch-id>

# 列出所有分支
neonctl branches list --project-id <project-id>
```

#### 使用分支

```bash
# 获取分支连接字符串
neonctl connection-string --project-id <project-id> --branch-name dev

# 在分支上测试
psql "postgresql://user:password@ep-dev-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
```

### 2.4 配置优化

#### 连接池配置

```bash
# 使用连接池（推荐）
# Neon 提供内置连接池，使用 pooler 端点
psql "postgresql://user:password@ep-xxx-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"

# 或使用 PgBouncer
# Neon 自动提供 PgBouncer 连接池
```

#### 性能优化

```sql
-- 启用查询缓存
SET enable_query_cache = on;

-- 优化连接数
-- Neon 自动管理连接池，无需手动配置
```

---

## 3. Supabase

### 3.1 概述

Supabase 提供完整的后端即服务（BaaS）平台：

- ✅ **PostgreSQL 数据库**: 托管 PostgreSQL
- ✅ **实时功能**: 实时订阅和更新
- ✅ **存储服务**: 对象存储集成
- ✅ **认证服务**: 用户认证和授权
- ✅ **边缘函数**: Serverless 函数支持

### 3.2 快速开始

#### 创建项目

```bash
# 安装 Supabase CLI
npm install -g supabase

# 登录
supabase login

# 创建项目（通过 Web 控制台）
# 访问 https://supabase.com/dashboard
```

#### 连接数据库

```bash
# 获取连接字符串
# 在 Supabase Dashboard 中获取

# 使用连接字符串
psql "postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres"
```

### 3.3 实时功能

```sql
-- 启用实时订阅
ALTER PUBLICATION supabase_realtime ADD TABLE messages;

-- 在应用中订阅
-- JavaScript 示例
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(SUPABASE_URL, SUPABASE_KEY)

// 订阅表变化
supabase
  .channel('messages')
  .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'messages' }, payload => {
    console.log('New message:', payload.new)
  })
  .subscribe()
```

### 3.4 存储集成

```sql
-- 创建存储桶
INSERT INTO storage.buckets (id, name, public)
VALUES ('avatars', 'avatars', true);

-- 上传文件（通过 API）
-- JavaScript 示例
const { data, error } = await supabase.storage
  .from('avatars')
  .upload('avatar.png', file)
```

---

## 4. AWS RDS Serverless v2

### 4.1 概述

AWS RDS Serverless v2 提供：

- ✅ **自动扩缩容**: 根据负载自动调整容量
- ✅ **高可用**: 多可用区部署
- ✅ **企业级安全**: VPC、加密、审计
- ✅ **按需计费**: 按实际使用量计费

### 4.2 创建 Serverless 集群

#### 使用 AWS CLI

```bash
# 创建 Serverless v2 集群
aws rds create-db-cluster \
  --db-cluster-identifier my-serverless-cluster \
  --engine postgres \
  --engine-version 18.0 \
  --serverless-v2-scaling-configuration MinCapacity=0.5,MaxCapacity=16 \
  --master-username postgres \
  --master-user-password MyPassword123 \
  --database-name mydb
```

#### 使用 Terraform

```hcl
resource "aws_rds_cluster" "serverless" {
  cluster_identifier      = "my-serverless-cluster"
  engine                 = "aurora-postgresql"
  engine_version         = "18.0"
  database_name          = "mydb"
  master_username       = "postgres"
  master_password       = var.db_password

  serverlessv2_scaling_configuration {
    min_capacity = 0.5
    max_capacity = 16
  }
}

resource "aws_rds_cluster_instance" "serverless" {
  identifier         = "my-serverless-instance"
  cluster_identifier = aws_rds_cluster.serverless.id
  instance_class     = "db.serverless"
  engine             = aws_rds_cluster.serverless.engine
  engine_version     = aws_rds_cluster.serverless.engine_version
}
```

### 4.3 配置自动扩缩容

```bash
# 更新扩缩容配置
aws rds modify-db-cluster \
  --db-cluster-identifier my-serverless-cluster \
  --serverless-v2-scaling-configuration MinCapacity=1,MaxCapacity=32
```

---

## 5. Azure Database for PostgreSQL Flexible Server

### 5.1 概述

Azure Flexible Server 提供：

- ✅ **灵活部署**: 单服务器或高可用部署
- ✅ **自动备份**: 自动备份和点对点恢复
- ✅ **监控和诊断**: 集成 Azure Monitor
- ✅ **安全功能**: 私有链接、防火墙规则

### 5.2 创建 Flexible Server

#### 使用 Azure CLI

```bash
# 创建资源组
az group create --name myResourceGroup --location eastus

# 创建 Flexible Server
az postgres flexible-server create \
  --resource-group myResourceGroup \
  --name mypostgresql \
  --location eastus \
  --admin-user postgres \
  --admin-password MyPassword123 \
  --sku-name Standard_B1ms \
  --tier Burstable \
  --version 18 \
  --storage-size 32 \
  --public-access 0.0.0.0
```

#### 使用 Azure Portal

1. 登录 Azure Portal
2. 创建 PostgreSQL Flexible Server 资源
3. 配置服务器参数
4. 设置防火墙规则
5. 获取连接字符串

### 5.3 配置自动扩缩容

```bash
# 更新服务器配置（自动扩缩容通过 SKU 调整）
az postgres flexible-server update \
  --resource-group myResourceGroup \
  --name mypostgresql \
  --sku-name Standard_B2s
```

---

## 6. 分支与开发工作流

### 6.1 Neon 分支工作流

```bash
# 1. 创建功能分支
neonctl branches create --project-id <project-id> --name feature-auth

# 2. 在分支上开发
psql "postgresql://user:password@ep-feature-auth-xxx.neon.tech/neondb"

# 3. 测试完成后合并到主分支
# （通过 Neon Dashboard 或 API）

# 4. 删除功能分支
neonctl branches delete --project-id <project-id> --branch-id <branch-id>
```

### 6.2 Supabase 分支工作流

```bash
# 1. 创建迁移
supabase migration new add_users_table

# 2. 在本地开发
supabase start

# 3. 推送到开发环境
supabase db push

# 4. 推送到生产环境
supabase db push --linked
```

### 6.3 最佳实践

- **分支命名**: 使用有意义的名称（feature-xxx, bugfix-xxx）
- **定期清理**: 删除不再需要的分支
- **数据隔离**: 每个分支使用独立的数据
- **测试验证**: 在分支上充分测试后再合并

---

## 7. 成本优化

### 7.1 Neon 成本优化

```bash
# 使用暂停功能（自动暂停）
# Neon 自动暂停不活跃的数据库

# 手动暂停
neonctl branches suspend --project-id <project-id> --branch-id <branch-id>

# 恢复
neonctl branches resume --project-id <project-id> --branch-id <branch-id>
```

### 7.2 AWS RDS Serverless 成本优化

```bash
# 设置最小容量（降低成本）
aws rds modify-db-cluster \
  --db-cluster-identifier my-serverless-cluster \
  --serverless-v2-scaling-configuration MinCapacity=0.5,MaxCapacity=8

# 使用预留容量（长期使用）
aws rds purchase-reserved-db-instances-offering \
  --reserved-db-instances-offering-id <offering-id>
```

### 7.3 通用优化建议

- **连接池**: 使用连接池减少连接数
- **查询优化**: 优化慢查询，减少计算资源
- **索引优化**: 合理使用索引，减少扫描
- **定期清理**: 清理不必要的数据和分支

---

## 8. 迁移到 Serverless

### 8.1 从自建 PostgreSQL 迁移

#### 使用 pg_dump

```bash
# 导出数据
pg_dump -h localhost -U postgres -d mydb -F c -f mydb.dump

# 导入到 Neon
pg_restore -h ep-xxx.neon.tech -U user -d neondb -F c mydb.dump
```

#### 使用逻辑复制

```sql
-- 在源数据库创建发布
CREATE PUBLICATION mypub FOR ALL TABLES;

-- 在目标数据库创建订阅
CREATE SUBSCRIPTION mysub
CONNECTION 'host=source_host port=5432 user=replicator password=password dbname=mydb'
PUBLICATION mypub;
```

### 8.2 迁移检查清单

- [ ] 备份源数据库
- [ ] 检查扩展兼容性
- [ ] 测试连接和性能
- [ ] 迁移数据
- [ ] 验证数据完整性
- [ ] 更新应用连接字符串
- [ ] 监控性能和成本

---

## 9. 深入阅读

### 相关文档

- [云原生与容器化](./05.12-云原生与容器化.md)
- [Kubernetes 部署指南](./05.13-Kubernetes部署.md)
- [Docker 部署指南](./05.12-Docker部署.md)
- [Serverless 与分支](../07-前沿技术/AI-时代/03-Serverless与分支-Neon与Supabase.md)

### 外部资源

- [Neon 官方文档](https://neon.tech/docs)
- [Supabase 官方文档](https://supabase.com/docs)
- [AWS RDS Serverless v2 文档](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-serverless-v2.html)
- [Azure Flexible Server 文档](https://docs.microsoft.com/azure/postgresql/flexible-server/)

---

**最后更新**: 2025-11-13

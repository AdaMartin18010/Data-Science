# 3.3.3 并发控制算法

## 📑 目录

- [3.3.3 并发控制算法](#333-并发控制算法)
  - [📑 目录](#-目录)
  - [1. 摘要](#1-摘要)
  - [3. 并发控制概述](#3-并发控制概述)
  - [4. 多版本并发控制(MVCC)](#4-多版本并发控制mvcc)
    - [4.1. 版本链管理](#41-版本链管理)
    - [4.2. 版本可见性判断](#42-版本可见性判断)
    - [4.3. 快照隔离实现](#43-快照隔离实现)
  - [5. 锁管理算法](#5-锁管理算法)
    - [5.1. 锁兼容矩阵](#51-锁兼容矩阵)
  - [6. 锁管理器](#6-锁管理器)
  - [7. 死锁检测算法](#7-死锁检测算法)
    - [7.1. 等待图构建](#71-等待图构建)
  - [8. 死锁检测与解决](#8-死锁检测与解决)
  - [9. 事务调度算法](#9-事务调度算法)
    - [9.1. 可串行化调度](#91-可串行化调度)
  - [10. 本地跳转与交叉引用](#10-本地跳转与交叉引用)
  - [11. 多表征](#11-多表征)
  - [12. 形式化语义](#12-形式化语义)
  - [13. 形式化语法与证明](#13-形式化语法与证明)
  - [14. 两阶段锁协议（2PL）](#14-两阶段锁协议2pl)
    - [14.1. 基本2PL](#141-基本2pl)
    - [14.2. 严格2PL](#142-严格2pl)
    - [14.3. 强2PL](#143-强2pl)
  - [15. 多粒度锁](#15-多粒度锁)
    - [15.1. 锁粒度层次](#151-锁粒度层次)
    - [15.2. 锁兼容矩阵](#152-锁兼容矩阵)
    - [15.3. 多粒度锁实现](#153-多粒度锁实现)
  - [16. 分布式并发控制](#16-分布式并发控制)
    - [16.1. 分布式两阶段提交（2PC）](#161-分布式两阶段提交2pc)
    - [16.2. 三阶段提交（3PC）](#162-三阶段提交3pc)
    - [16.3. Paxos算法](#163-paxos算法)
  - [17. 实际应用案例](#17-实际应用案例)
    - [17.1. PostgreSQL MVCC](#171-postgresql-mvcc)
    - [17.2. MySQL InnoDB](#172-mysql-innodb)
    - [17.3. Oracle数据库](#173-oracle数据库)
  - [18. 性能优化](#18-性能优化)
    - [18.1. 锁优化](#181-锁优化)
    - [18.2. 死锁预防](#182-死锁预防)
    - [18.3. 并发度优化](#183-并发度优化)
  - [19. 最佳实践](#19-最佳实践)
    - [19.1. 事务设计](#191-事务设计)
    - [19.2. 锁使用](#192-锁使用)
    - [19.3. 性能监控](#193-性能监控)
  - [20. 总结](#20-总结)

---


## 1. 摘要

本文件系统梳理数据库并发控制的核心算法，以PostgreSQL的MVCC机制为例，涵盖多版本并发控制、锁管理、死锁检测、事务调度等关键技术。

---

## 3. 并发控制概述

并发控制确保多个事务并发执行时，数据库保持一致性。

**核心问题**：

- 脏读 (Dirty Read)
- 不可重复读 (Non-repeatable Read)
- 幻读 (Phantom Read)
- 丢失更新 (Lost Update)

**隔离级别**：

- READ UNCOMMITTED
- READ COMMITTED
- REPEATABLE READ
- SERIALIZABLE

## 4. 多版本并发控制(MVCC)

### 4.1. 版本链管理

```rust
// Rust实现：版本链结构
#[derive(Debug, Clone)]
struct Version {
    xmin: TransactionId,    // 创建该版本的事务ID
    xmax: TransactionId,    // 删除该版本的事务ID
    cid: CommandId,         // 命令ID
    data: Vec<u8>,          // 实际数据
    next: Option<Box<Version>>, // 下一个版本
}

#[derive(Debug)]
struct MVCCRecord {
    versions: Option<Box<Version>>, // 版本链头
    current: Option<Box<Version>>,  // 当前可见版本
}
```

### 4.2. 版本可见性判断

```rust
// Rust实现：版本可见性判断
impl MVCCRecord {
    fn is_visible(&self, version: &Version, snapshot: &Snapshot) -> bool {
        // 检查版本是否对当前快照可见
        if version.xmax != INVALID_TXN_ID &&
           snapshot.is_active(version.xmax) {
            return false; // 被未提交事务删除
        }

        if !snapshot.is_active(version.xmin) {
            return false; // 创建事务未提交
        }

        true
    }

    fn get_visible_version(&self, snapshot: &Snapshot) -> Option<&Version> {
        let mut current = self.versions.as_ref();
        while let Some(version) = current {
            if self.is_visible(version, snapshot) {
                return Some(version);
            }
            current = version.next.as_ref();
        }
        None
    }
}
```

### 4.3. 快照隔离实现

```go
// Go实现：快照隔离
type Snapshot struct {
    xmin    TransactionId
    xmax    TransactionId
    xip     map[TransactionId]bool // 活跃事务列表
}

type Transaction struct {
    id        TransactionId
    snapshot  *Snapshot
    status    TransactionStatus
    startTime time.Time
}

func (t *Transaction) Begin() {
    t.snapshot = t.createSnapshot()
    t.status = ACTIVE
}

func (t *Transaction) createSnapshot() *Snapshot {
    return &Snapshot{
        xmin: getOldestActiveTxn(),
        xmax: getNextTxnId(),
        xip:  getActiveTxnList(),
    }
}
```

## 5. 锁管理算法

### 5.1. 锁兼容矩阵

```python
# Python实现：锁兼容矩阵
class LockType:
    SHARED = 0
    EXCLUSIVE = 1
    INTENT_SHARED = 2
    INTENT_EXCLUSIVE = 3
    SHARED_INTENT_EXCLUSIVE = 4

# 锁兼容矩阵
LOCK_COMPATIBILITY = {
    LockType.SHARED: [True, False, True, False, False],
    LockType.EXCLUSIVE: [False, False, False, False, False],
    LockType.INTENT_SHARED: [True, False, True, True, True],
    LockType.INTENT_EXCLUSIVE: [False, False, True, True, False],
    LockType.SHARED_INTENT_EXCLUSIVE: [False, False, True, False, False]
}

def is_compatible(lock1, lock2):
    return LOCK_COMPATIBILITY[lock1][lock2]
```

## 6. 锁管理器

```go
// Go实现：锁管理器
type LockManager struct {
    locks map[string]*LockQueue
    mu    sync.RWMutex
}

type LockQueue struct {
    granted []*LockRequest
    waiting []*LockRequest
    mu      sync.Mutex
}

type LockRequest struct {
    txnId    TransactionId
    lockType LockType
    resource string
    granted  bool
    cond     *sync.Cond
}

func (lm *LockManager) AcquireLock(txnId TransactionId, resource string, lockType LockType) bool {
    lm.mu.Lock()
    queue, exists := lm.locks[resource]
    if !exists {
        queue = &LockQueue{}
        lm.locks[resource] = queue
    }
    lm.mu.Unlock()

    return queue.acquireLock(txnId, lockType)
}

func (lq *LockQueue) acquireLock(txnId TransactionId, lockType LockType) bool {
    lq.mu.Lock()
    defer lq.mu.Unlock()

    // 检查是否可以立即获得锁
    if lq.canGrantLock(lockType) {
        lq.granted = append(lq.granted, &LockRequest{
            txnId:    txnId,
            lockType: lockType,
            granted:  true,
        })
        return true
    }

    // 加入等待队列
    request := &LockRequest{
        txnId:    txnId,
        lockType: lockType,
        granted:  false,
        cond:     sync.NewCond(&lq.mu),
    }
    lq.waiting = append(lq.waiting, request)

    // 等待锁被授予
    for !request.granted {
        request.cond.Wait()
    }

    return true
}
```

## 7. 死锁检测算法

### 7.1. 等待图构建

```python
# Python实现：等待图构建
class WaitForGraph:
    def __init__(self):
        self.graph = defaultdict(set)
        self.reverse_graph = defaultdict(set)

    def add_edge(self, from_txn, to_txn):
        """添加等待边：from_txn 等待 to_txn"""
        self.graph[from_txn].add(to_txn)
        self.reverse_graph[to_txn].add(from_txn)

    def remove_edge(self, from_txn, to_txn):
        """移除等待边"""
        self.graph[from_txn].discard(to_txn)
        self.reverse_graph[to_txn].discard(from_txn)

    def detect_cycle(self):
        """检测环（死锁）"""
        visited = set()
        rec_stack = set()

        def dfs(node):
            visited.add(node)
            rec_stack.add(node)

            for neighbor in self.graph[node]:
                if neighbor not in visited:
                    if dfs(neighbor):
                        return True
                elif neighbor in rec_stack:
                    return True

            rec_stack.remove(node)
            return False

        for node in self.graph:
            if node not in visited:
                if dfs(node):
                    return True
        return False
```

## 8. 死锁检测与解决

```go
// Go实现：死锁检测与解决
type DeadlockDetector struct {
    waitForGraph *WaitForGraph
    txnManager   *TransactionManager
}

func (dd *DeadlockDetector) DetectAndResolve() {
    if dd.waitForGraph.HasCycle() {
        // 找到死锁环
        cycle := dd.waitForGraph.FindCycle()

        // 选择牺牲者（通常选择最年轻的事务）
        victim := dd.selectVictim(cycle)

        // 中止牺牲者事务
        dd.txnManager.AbortTransaction(victim)

        // 移除相关的等待边
        dd.waitForGraph.RemoveTransactionEdges(victim)
    }
}

func (dd *DeadlockDetector) selectVictim(cycle []TransactionId) TransactionId {
    // 选择策略：最年轻的事务
    var youngest TransactionId
    var youngestTime time.Time

    for _, txnId := range cycle {
        txn := dd.txnManager.GetTransaction(txnId)
        if txn.StartTime.After(youngestTime) {
            youngest = txnId
            youngestTime = txn.StartTime
        }
    }

    return youngest
}
```

## 9. 事务调度算法

### 9.1. 可串行化调度

```python
# Python实现：可串行化调度检测
class SerializableScheduler:
    def __init__(self):
        self.conflict_graph = defaultdict(set)

    def add_operation(self, txn_id, operation, resource):
        """添加操作到调度"""
# 检查与之前操作的冲突
        for prev_txn, prev_op, prev_resource in self.operations:
            if self.has_conflict(operation, prev_op, resource, prev_resource):
# 添加冲突边
                self.conflict_graph[prev_txn].add(txn_id)

        self.operations.append((txn_id, operation, resource))

    def has_conflict(self, op1, op2, res1, res2):
        """检查两个操作是否有冲突"""
        if res1 != res2:
            return False

# 读写冲突
        if (op1 == 'R' and op2 == 'W') or (op1 == 'W' and op2 == 'R'):
            return True

# 写写冲突
        if op1 == 'W' and op2 == 'W':
            return True

        return False

    def is_serializable(self):
        """检查调度是否可串行化"""
        return not self.has_cycle()

    def has_cycle(self):
        """检测冲突图中是否有环"""
        visited = set()
        rec_stack = set()

        def dfs(node):
            visited.add(node)
            rec_stack.add(node)

            for neighbor in self.conflict_graph[node]:
                if neighbor not in visited:
                    if dfs(neighbor):
                        return True
                elif neighbor in rec_stack:
                    return True

            rec_stack.remove(node)
            return False

        for node in self.conflict_graph:
            if node not in visited:
                if dfs(node):
                    return True
        return False
```

## 10. 本地跳转与交叉引用

- [返回核心数据处理算法](./3.3.1-核心数据处理算法.md)
- [跳转到查询优化算法](./3.3.2-查询优化算法.md)
- [跳转到PostgreSQL MVCC分析](../../1-数据库系统/1.1-PostgreSQL/1.1.8-MVCC高级分析与形式证明.md)
- [跳转到数据模型形式化理论](../3.2-形式化模型/3.2.1-数据模型的形式化理论.md)

---

**最后更新**: 2024年12月
**版本**: v1.0
**状态**: ✅ 已完成

## 11. 多表征

并发控制算法支持多种表征方式，包括：

- 符号表征（并发协议、锁机制、调度算法、伪代码等）
- 图结构（锁依赖图、调度流程图、冲突图等）
- 向量/张量（状态向量、参数矩阵、特征嵌入）
- 自然语言（定义、注释、描述）
- 图像/可视化（流程图、结构图、并发可视化等）
这些表征可互映，提升并发控制算法的表达力。

## 12. 形式化语义

- 语义域：$D$，如事务对象集、锁空间、调度模型空间
- 解释函数：$I: S \to D$，将符号/结构映射到具体语义对象
- 语义一致性：每个协议/调度/公式在$D$中有明确定义

## 13. 形式化语法与证明

- 语法规则：如协议定义、调度规则、推理规则、约束条件
- **定理**：并发控制算法的语法系统具一致性与可扩展性。
- **证明**：由协议定义、调度规则与推理规则递归定义，保证系统一致与可扩展。

---

## 14. 两阶段锁协议（2PL）

### 14.1. 基本2PL

**协议规则**：

1. **增长阶段**：事务只能获取锁，不能释放锁
2. **收缩阶段**：事务只能释放锁，不能获取锁

**实现**：

```python
class TwoPhaseLocking:
    def __init__(self):
        self.transactions = {}
        self.lock_manager = LockManager()
        self.phase = {}  # 事务阶段：'growing' 或 'shrinking'

    def begin_transaction(self, txn_id):
        self.transactions[txn_id] = {
            'id': txn_id,
            'locks': [],
            'phase': 'growing'
        }
        self.phase[txn_id] = 'growing'

    def acquire_lock(self, txn_id, resource_id, lock_type):
        if self.phase[txn_id] == 'shrinking':
            raise ValueError("事务已进入收缩阶段，不能获取新锁")

        if self.lock_manager.acquire_lock(txn_id, resource_id, lock_type):
            self.transactions[txn_id]['locks'].append((resource_id, lock_type))
            return True
        return False

    def release_lock(self, txn_id, resource_id):
        if self.phase[txn_id] == 'growing':
            self.phase[txn_id] = 'shrinking'

        self.lock_manager.release_lock(txn_id, resource_id)
        self.transactions[txn_id]['locks'] = [
            (rid, lt) for rid, lt in self.transactions[txn_id]['locks']
            if rid != resource_id
        ]

    def commit(self, txn_id):
        # 释放所有锁
        for resource_id, _ in self.transactions[txn_id]['locks']:
            self.lock_manager.release_lock(txn_id, resource_id)
        del self.transactions[txn_id]
        del self.phase[txn_id]
```

### 14.2. 严格2PL

**特点**：

- 所有锁在事务提交时释放
- 避免级联回滚
- 保证可恢复性

### 14.3. 强2PL

**特点**：

- 排他锁在事务提交时释放
- 共享锁可以提前释放
- 平衡性能和安全性

---

## 15. 多粒度锁

### 15.1. 锁粒度层次

**层次结构**：

```text
数据库
  └── 表
      └── 页
          └── 行
```

**锁类型**：

- **意向共享锁（IS）**：表示在较低级别获取共享锁的意向
- **意向排他锁（IX）**：表示在较低级别获取排他锁的意向
- **共享意向排他锁（SIX）**：共享锁 + 意向排他锁

### 15.2. 锁兼容矩阵

|      | IS  | IX  | S   | SIX | X   |
|------|-----|-----|-----|-----|-----|
| IS   | ✅  | ✅  | ✅  | ✅  | ❌  |
| IX   | ✅  | ✅  | ❌  | ❌  | ❌  |
| S    | ✅  | ❌  | ✅  | ❌  | ❌  |
| SIX  | ✅  | ❌  | ❌  | ❌  | ❌  |
| X    | ❌  | ❌  | ❌  | ❌  | ❌  |

### 15.3. 多粒度锁实现

```python
class MultiGranularityLock:
    def __init__(self, resource_id, granularity):
        self.resource_id = resource_id
        self.granularity = granularity  # 'database', 'table', 'page', 'row'
        self.intention_locks = set()  # IS, IX, SIX
        self.explicit_locks = set()   # S, X
        self.children = {}  # 子资源锁

    def acquire_intention_lock(self, txn_id, lock_type):
        """获取意向锁"""
        if self._is_compatible_intention(lock_type):
            self.intention_locks.add((txn_id, lock_type))
            return True
        return False

    def acquire_explicit_lock(self, txn_id, lock_type):
        """获取显式锁"""
        if self._is_compatible_explicit(lock_type):
            self.explicit_locks.add((txn_id, lock_type))
            return True
        return False

    def _is_compatible_intention(self, lock_type):
        """检查意向锁兼容性"""
        for _, existing_lock in self.intention_locks:
            if not self._compatible(lock_type, existing_lock):
                return False
        return True

    def _is_compatible_explicit(self, lock_type):
        """检查显式锁兼容性"""
        for _, existing_lock in self.explicit_locks:
            if not self._compatible(lock_type, existing_lock):
                return False
        return True
```

---

## 16. 分布式并发控制

### 16.1. 分布式两阶段提交（2PC）

**阶段1：准备阶段**：

1. 协调者向所有参与者发送PREPARE消息
2. 参与者执行事务，写入日志，但不提交
3. 参与者向协调者发送YES或NO

**阶段2：提交阶段**：

1. 如果所有参与者都返回YES，协调者发送COMMIT
2. 否则，协调者发送ABORT
3. 参与者根据消息提交或回滚

**实现**：

```python
class TwoPhaseCommit:
    def __init__(self, coordinator, participants):
        self.coordinator = coordinator
        self.participants = participants
        self.state = 'INIT'

    def prepare(self):
        """准备阶段"""
        self.state = 'PREPARING'
        votes = []

        for participant in self.participants:
            vote = participant.prepare()
            votes.append(vote)

        if all(vote == 'YES' for vote in votes):
            return self.commit()
        else:
            return self.abort()

    def commit(self):
        """提交阶段"""
        self.state = 'COMMITTING'
        for participant in self.participants:
            participant.commit()
        self.state = 'COMMITTED'
        return 'COMMITTED'

    def abort(self):
        """中止阶段"""
        self.state = 'ABORTING'
        for participant in self.participants:
            participant.abort()
        self.state = 'ABORTED'
        return 'ABORTED'
```

### 16.2. 三阶段提交（3PC）

**改进**：

- 增加预提交阶段
- 避免阻塞
- 提高可用性

**阶段**：

1. **CanCommit**：询问是否可以提交
2. **PreCommit**：预提交，准备提交
3. **DoCommit**：执行提交

### 16.3. Paxos算法

**应用场景**：

- 分布式一致性
- 选主算法
- 状态复制

**算法流程**：

1. **Prepare阶段**：提议者发送prepare请求
2. **Promise阶段**：接受者响应promise
3. **Accept阶段**：提议者发送accept请求
4. **Accepted阶段**：接受者响应accepted

---

## 17. 实际应用案例

### 17.1. PostgreSQL MVCC

**实现特点**：

- 使用版本链管理多版本
- 快照隔离实现
- 可见性判断算法
- 垃圾回收机制

**性能优化**：

- 版本链优化
- 快照优化
- 垃圾回收优化

### 17.2. MySQL InnoDB

**实现特点**：

- 行级锁
- 多版本并发控制
- 间隙锁防止幻读
- 死锁检测

### 17.3. Oracle数据库

**实现特点**：

- 多版本读一致性
- 行级锁
- 自动死锁检测
- 锁升级机制

---

## 18. 性能优化

### 18.1. 锁优化

**优化策略**：

- **锁粒度选择**：根据访问模式选择合适粒度
- **锁超时**：设置锁超时避免长时间等待
- **锁升级**：细粒度锁升级为粗粒度锁
- **锁降级**：粗粒度锁降级为细粒度锁

### 18.2. 死锁预防

**预防策略**：

- **锁排序**：按固定顺序获取锁
- **超时机制**：设置锁超时
- **死锁检测**：定期检测死锁
- **死锁避免**：使用银行家算法

### 18.3. 并发度优化

**优化方法**：

- **MVCC**：提高读并发度
- **读写分离**：分离读写操作
- **分区**：减少锁竞争
- **无锁数据结构**：使用CAS操作

---

## 19. 最佳实践

### 19.1. 事务设计

**设计原则**：

- 事务尽可能短
- 避免长事务
- 减少锁持有时间
- 合理使用隔离级别

### 19.2. 锁使用

**使用建议**：

- 选择合适的锁粒度
- 避免死锁
- 使用超时机制
- 监控锁竞争

### 19.3. 性能监控

**监控指标**：

- 锁等待时间
- 死锁频率
- 事务吞吐量
- 并发度

---

## 20. 总结

并发控制是数据库系统的核心功能，通过锁机制、MVCC、时间戳排序等技术，确保多个事务并发执行时数据库的一致性和正确性。随着分布式系统的发展，分布式并发控制技术也在不断演进。

**核心价值**：

1. **数据一致性**：保证数据一致性和正确性
2. **并发性能**：提高系统并发性能
3. **隔离性**：提供不同级别的隔离性
4. **可靠性**：保证系统可靠性

**未来展望**：

随着分布式系统、云数据库等技术的发展，并发控制技术将继续发展，特别是在分布式一致性、无锁数据结构、高性能并发控制等领域，并发控制将提供更强大的功能和更好的性能。

---

---

## 16. 参考资料

### 16.1. 相关理论文档

- [算法实现分支导航](./README.md) - 算法实现完整知识体系
- [数据模型与算法知识导航索引](../README.md) - 数据模型与算法知识导航
- [核心数据处理算法](./3.3.1-核心数据处理算法.md) - 核心数据处理算法
- [查询优化算法](./3.3.2-查询优化算法.md) - 查询优化算法
- [Petri网理论](../../2-形式科学理论/2.3-Petri网理论/README.md) - Petri网理论基础
- [时态逻辑控制理论](../../2-形式科学理论/2.4-时态逻辑控制理论/README.md) - 时态逻辑控制理论
- [分布式系统理论](../../2-形式科学理论/2.5-分布式系统理论/README.md) - 分布式系统理论基础
- [数据库性能调优实战](../../../Design/07-数据库设计实践/07.08-数据库性能调优实战.md) - 数据库性能调优实践

### 16.2. 相关资源

- [并发控制资源](https://en.wikipedia.org/wiki/Concurrency_control)
- [事务处理](https://en.wikipedia.org/wiki/Transaction_processing)

---

[返回上级目录](../README.md)

# 3.5.6 数据结构转换与建模

## 📑 目录

- [3.5.6 数据结构转换与建模](#356-数据结构转换与建模)
  - [📑 目录](#-目录)
  - [1. 概述](#1-概述)
    - [1.1. 数据结构类型](#11-数据结构类型)
    - [1.2. 转换与建模意义](#12-转换与建模意义)
  - [2. 数据结构类型](#2-数据结构类型)
    - [2.1. 结构化数据](#21-结构化数据)
      - [2.1.1. 结构化数据定义](#211-结构化数据定义)
    - [2.2. 半结构化数据](#22-半结构化数据)
      - [2.2.1. 半结构化数据定义](#221-半结构化数据定义)
    - [2.3. 非结构化数据](#23-非结构化数据)
      - [2.3.1. 非结构化数据定义](#231-非结构化数据定义)
  - [3. 数据结构转换方法](#3-数据结构转换方法)
    - [3.1. JSON转换](#31-json转换)
      - [3.1.1. JSON到DataFrame](#311-json到dataframe)
      - [3.1.2. DataFrame到JSON](#312-dataframe到json)
    - [3.2. XML转换](#32-xml转换)
      - [3.2.1. XML到DataFrame](#321-xml到dataframe)
    - [3.3. CSV转换](#33-csv转换)
      - [3.3.1. CSV读写](#331-csv读写)
      - [3.3.2. CSV到JSON](#332-csv到json)
    - [3.4. Parquet转换](#34-parquet转换)
      - [3.4.1. Parquet读写](#341-parquet读写)
      - [3.4.2. Parquet优势](#342-parquet优势)
  - [4. 数据规范化](#4-数据规范化)
    - [4.1. 规范化原则](#41-规范化原则)
    - [4.2. 规范化方法](#42-规范化方法)
      - [4.2.1. 数据清洗](#421-数据清洗)
      - [4.2.2. 数据类型转换](#422-数据类型转换)
      - [4.2.3. 数据标准化](#423-数据标准化)
  - [5. 数据建模](#5-数据建模)
    - [5.1. 关系模型](#51-关系模型)
      - [5.1.1. 关系模型定义](#511-关系模型定义)
    - [5.2. 文档模型](#52-文档模型)
      - [5.2.1. 文档模型定义](#521-文档模型定义)
    - [5.3. 图模型](#53-图模型)
      - [5.3.1. 图模型定义](#531-图模型定义)
  - [6. 实际应用案例](#6-实际应用案例)
    - [6.1. 医疗行业案例](#61-医疗行业案例)
    - [6.2. 金融行业案例](#62-金融行业案例)
  - [7. 形式化定义](#7-形式化定义)
    - [7.1. 数据结构转换形式化](#71-数据结构转换形式化)
    - [7.2. 数据建模形式化](#72-数据建模形式化)
  - [8. 多表征](#8-多表征)
  - [9. 总结与展望](#9-总结与展望)
    - [9.1. 总结](#91-总结)
    - [9.2. 发展趋势](#92-发展趋势)

## 1. 概述

### 1.1. 数据结构类型

**数据结构类型分类**：

1. **结构化数据**：关系型数据库、表格数据
2. **半结构化数据**：JSON、XML、HTML
3. **非结构化数据**：文本、图像、视频

### 1.2. 转换与建模意义

**数据结构转换与建模的意义**：

1. **数据统一**：统一数据格式
2. **提高效率**：提高数据处理效率
3. **便于分析**：便于数据分析和建模
4. **数据质量**：提高数据质量

---

## 2. 数据结构类型

### 2.1. 结构化数据

#### 2.1.1. 结构化数据定义

**结构化数据**：具有固定模式的数据，通常存储在关系型数据库中。

**特点**：

- **固定模式**：固定的数据结构
- **易于查询**：易于SQL查询
- **规范化**：高度规范化

**示例**：

```python
import pandas as pd

# 结构化数据示例
data = {
    'id': [1, 2, 3],
    'name': ['Alice', 'Bob', 'Charlie'],
    'age': [25, 30, 35]
}

df = pd.DataFrame(data)
print(df)
```

### 2.2. 半结构化数据

#### 2.2.1. 半结构化数据定义

**半结构化数据**：具有一定结构但不完全固定的数据。

**特点**：

- **灵活模式**：灵活的数据结构
- **嵌套结构**：支持嵌套结构
- **自描述**：数据包含结构信息

**JSON示例**：

```python
import json

# JSON数据示例
json_data = {
    "user": {
        "id": 1,
        "name": "Alice",
        "contacts": [
            {"type": "email", "value": "alice@example.com"},
            {"type": "phone", "value": "123-456-7890"}
        ]
    }
}

json_string = json.dumps(json_data, indent=2)
print(json_string)
```

**XML示例**：

```python
import xml.etree.ElementTree as ET

# XML数据示例
xml_data = """
<user>
    <id>1</id>
    <name>Alice</name>
    <contacts>
        <contact type="email">alice@example.com</contact>
        <contact type="phone">123-456-7890</contact>
    </contacts>
</user>
"""

root = ET.fromstring(xml_data)
print(root.find('name').text)
```

### 2.3. 非结构化数据

#### 2.3.1. 非结构化数据定义

**非结构化数据**：没有固定结构的数据。

**特点**：

- **无固定模式**：没有固定的数据结构
- **多样化**：文本、图像、视频等
- **需要解析**：需要特殊处理

---

## 3. 数据结构转换方法

### 3.1. JSON转换

#### 3.1.1. JSON到DataFrame

**JSON转DataFrame**：

```python
import json
import pandas as pd

# JSON数据
json_data = [
    {"id": 1, "name": "Alice", "age": 25},
    {"id": 2, "name": "Bob", "age": 30},
    {"id": 3, "name": "Charlie", "age": 35}
]

# 转换为DataFrame
df = pd.DataFrame(json_data)
print(df)
```

#### 3.1.2. DataFrame到JSON

**DataFrame转JSON**：

```python
import pandas as pd

# DataFrame数据
df = pd.DataFrame({
    'id': [1, 2, 3],
    'name': ['Alice', 'Bob', 'Charlie'],
    'age': [25, 30, 35]
})

# 转换为JSON
json_string = df.to_json(orient='records', indent=2)
print(json_string)
```

### 3.2. XML转换

#### 3.2.1. XML到DataFrame

**XML转DataFrame**：

```python
import xml.etree.ElementTree as ET
import pandas as pd

# XML数据
xml_string = """
<users>
    <user>
        <id>1</id>
        <name>Alice</name>
        <age>25</age>
    </user>
    <user>
        <id>2</id>
        <name>Bob</name>
        <age>30</age>
    </user>
</users>
"""

root = ET.fromstring(xml_string)
data = []
for user in root.findall('user'):
    data.append({
        'id': int(user.find('id').text),
        'name': user.find('name').text,
        'age': int(user.find('age').text)
    })

df = pd.DataFrame(data)
print(df)
```

### 3.3. CSV转换

#### 3.3.1. CSV读写

**CSV读写**：

```python
import pandas as pd

# 读取CSV
df = pd.read_csv('data.csv')

# 写入CSV
df.to_csv('output.csv', index=False)

# 自定义分隔符
df.to_csv('output.tsv', sep='\t', index=False)
```

#### 3.3.2. CSV到JSON

**CSV转JSON**：

```python
import pandas as pd
import json

# 读取CSV
df = pd.read_csv('data.csv')

# 转换为JSON
json_data = df.to_json(orient='records')
print(json_data)
```

### 3.4. Parquet转换

#### 3.4.1. Parquet读写

**Parquet读写**：

```python
import pandas as pd

# 读取Parquet
df = pd.read_parquet('data.parquet')

# 写入Parquet
df.to_parquet('output.parquet', compression='snappy')

# 指定分区
df.to_parquet('output.parquet', partition_cols=['year', 'month'])
```

#### 3.4.2. Parquet优势

**Parquet优势**：

- **列式存储**：列式存储格式
- **压缩率高**：高压缩率
- **查询效率**：查询效率高

---

## 4. 数据规范化

### 4.1. 规范化原则

**数据规范化原则**：

1. **唯一性**：确保数据唯一性
2. **一致性**：保持数据一致性
3. **完整性**：保证数据完整性
4. **准确性**：确保数据准确性

### 4.2. 规范化方法

#### 4.2.1. 数据清洗

**数据清洗**：

```python
import pandas as pd
import numpy as np

# 数据清洗示例
df = pd.DataFrame({
    'id': [1, 2, 3, 4, 5],
    'name': ['Alice', 'Bob', None, 'Charlie', ''],
    'age': [25, 30, 35, None, 40],
    'email': ['alice@example.com', 'bob@example.com', 'charlie@example.com', None, 'dave@example.com']
})

# 处理缺失值
df['name'] = df['name'].fillna('Unknown')
df['age'] = df['age'].fillna(df['age'].mean())

# 处理空字符串
df['name'] = df['name'].replace('', 'Unknown')

# 去重
df = df.drop_duplicates()

print(df)
```

#### 4.2.2. 数据类型转换

**数据类型转换**：

```python
import pandas as pd

# 数据类型转换
df = pd.DataFrame({
    'id': ['1', '2', '3'],
    'age': ['25', '30', '35'],
    'price': ['10.5', '20.3', '30.7']
})

# 转换数据类型
df['id'] = df['id'].astype(int)
df['age'] = df['age'].astype(int)
df['price'] = df['price'].astype(float)

print(df.dtypes)
```

#### 4.2.3. 数据标准化

**数据标准化**：

```python
import pandas as pd
from sklearn.preprocessing import StandardScaler

# 数据标准化
df = pd.DataFrame({
    'feature1': [1, 2, 3, 4, 5],
    'feature2': [10, 20, 30, 40, 50]
})

scaler = StandardScaler()
df_scaled = pd.DataFrame(
    scaler.fit_transform(df),
    columns=df.columns
)

print(df_scaled)
```

---

## 5. 数据建模

### 5.1. 关系模型

#### 5.1.1. 关系模型定义

**关系模型**：基于关系代数的数据模型。

**特点**：

- **表结构**：二维表结构
- **规范化**：高度规范化
- **完整性约束**：完整性约束

**示例**：

```python
import pandas as pd

# 关系模型示例
users = pd.DataFrame({
    'user_id': [1, 2, 3],
    'name': ['Alice', 'Bob', 'Charlie'],
    'email': ['alice@example.com', 'bob@example.com', 'charlie@example.com']
})

orders = pd.DataFrame({
    'order_id': [1, 2, 3],
    'user_id': [1, 2, 1],
    'amount': [100, 200, 150]
})

# 关联查询
result = pd.merge(users, orders, on='user_id')
print(result)
```

### 5.2. 文档模型

#### 5.2.1. 文档模型定义

**文档模型**：基于文档的数据模型。

**特点**：

- **嵌套结构**：支持嵌套结构
- **灵活模式**：灵活的数据模式
- **自包含**：文档自包含

**示例**：

```python
import json

# 文档模型示例
document = {
    "user_id": 1,
    "name": "Alice",
    "orders": [
        {
            "order_id": 1,
            "amount": 100,
            "items": [
                {"product": "Book", "quantity": 2},
                {"product": "Pen", "quantity": 5}
            ]
        },
        {
            "order_id": 2,
            "amount": 150,
            "items": [
                {"product": "Notebook", "quantity": 3}
            ]
        }
    ]
}

print(json.dumps(document, indent=2))
```

### 5.3. 图模型

#### 5.3.1. 图模型定义

**图模型**：基于图的数据模型。

**特点**：

- **节点和边**：节点和边结构
- **关系表达**：表达复杂关系
- **图查询**：支持图查询

**示例**：

```python
import networkx as nx

# 图模型示例
G = nx.Graph()

# 添加节点
G.add_node(1, name='Alice', age=25)
G.add_node(2, name='Bob', age=30)
G.add_node(3, name='Charlie', age=35)

# 添加边
G.add_edge(1, 2, relation='friend')
G.add_edge(2, 3, relation='colleague')
G.add_edge(1, 3, relation='friend')

# 查询
print(f"Nodes: {list(G.nodes(data=True))}")
print(f"Edges: {list(G.edges(data=True))}")
```

---

## 6. 实际应用案例

### 6.1. 医疗行业案例

**场景**：

- 电子病历（EMR）数据从XML/JSON转换为结构化表，便于分析与建模
- 典型流程：数据采集、格式转换、规范化、入库

**实现**：

```python
import json
import pandas as pd
from datetime import datetime

# 电子病历JSON数据
emr_json = {
    "patient_id": "P001",
    "name": "John Doe",
    "birth_date": "1980-01-01",
    "visits": [
        {
            "visit_date": "2024-01-15",
            "diagnosis": "Hypertension",
            "medications": ["Lisinopril", "Amlodipine"]
        },
        {
            "visit_date": "2024-02-20",
            "diagnosis": "Diabetes",
            "medications": ["Metformin"]
        }
    ]
}

# 转换为结构化表
visits_data = []
for visit in emr_json['visits']:
    visits_data.append({
        'patient_id': emr_json['patient_id'],
        'visit_date': visit['visit_date'],
        'diagnosis': visit['diagnosis'],
        'medications': ', '.join(visit['medications'])
    })

df_visits = pd.DataFrame(visits_data)
print(df_visits)
```

### 6.2. 金融行业案例

**场景**：

- 交易流水数据从多格式（CSV、JSON）归一化为分析型表结构
- 典型流程：多源采集、数据清洗、结构转换、建模

**实现**：

```python
import pandas as pd
from datetime import datetime

# 多源数据
csv_data = pd.read_csv('transactions.csv')
json_data = pd.read_json('transactions.json')

# 数据规范化
def normalize_transaction(df):
    """规范化交易数据"""
    df['transaction_date'] = pd.to_datetime(df['transaction_date'])
    df['amount'] = pd.to_numeric(df['amount'], errors='coerce')
    df['transaction_type'] = df['transaction_type'].str.upper()
    return df

# 规范化并合并
csv_normalized = normalize_transaction(csv_data)
json_normalized = normalize_transaction(json_data)

# 合并数据
all_transactions = pd.concat([csv_normalized, json_normalized], ignore_index=True)

# 数据建模：按日期和类型聚合
summary = all_transactions.groupby(['transaction_date', 'transaction_type']).agg({
    'amount': ['sum', 'count', 'mean']
}).reset_index()

print(summary)
```

---

## 7. 形式化定义

### 7.1. 数据结构转换形式化

**数据结构转换定义**：

设转换函数 $T: D_1 \to D_2$，其中：

- $D_1$：源数据结构
- $D_2$：目标数据结构

转换保持数据语义：

$$\forall d \in D_1, \text{Semantics}(d) = \text{Semantics}(T(d))$$

### 7.2. 数据建模形式化

**数据模型定义**：

数据模型 $M = (S, C, O)$，其中：

- $S$：结构定义
- $C$：约束条件
- $O$：操作集合

---

## 8. 多表征

本主题支持多种表征方式：

1. **符号表征**：形式化定义、数学公式
2. **图结构**：数据流程图、ER图、图模型
3. **代码实现**：转换代码示例
4. **自然语言**：概念定义、转换方法
5. **可视化**：数据转换可视化、模型可视化

---

## 9. 总结与展望

### 9.1. 总结

数据结构转换与建模的核心要点：

1. **数据结构类型**：结构化、半结构化、非结构化
2. **转换方法**：JSON、XML、CSV、Parquet转换
3. **数据规范化**：清洗、类型转换、标准化
4. **数据建模**：关系模型、文档模型、图模型

### 9.2. 发展趋势

**未来发展方向**：

1. **自动化转换**：自动化数据转换
2. **智能建模**：AI驱动的数据建模
3. **统一格式**：统一数据格式标准

---

**参考文献**：

1. Codd, E. F. (1970). "A Relational Model of Data for Large Shared Data Banks"
2. Abiteboul, S., et al. (2000). "Data on the Web: From Relations to Semistructured Data and XML"

---

[返回数据分析与ETL导航](../README.md)

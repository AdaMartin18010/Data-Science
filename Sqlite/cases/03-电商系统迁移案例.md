# 电商系统迁移案例：SQLite到PostgreSQL迁移实践

> **案例类型**：数据库迁移案例
> **迁移规模**：中等规模（50GB数据）
> **迁移时间**：2024年
> **最后更新**：2025-01-15

---

## 1. 📑 目录

- [电商系统迁移案例：SQLite到PostgreSQL迁移实践](#电商系统迁移案例sqlite到postgresql迁移实践)
  - [1. 📑 目录](#1--目录)
  - [2. 📋 案例概述](#2--案例概述)
  - [3. 🎯 业务背景](#3--业务背景)
    - [3.1. 系统现状](#31-系统现状)
    - [3.2. 迁移原因](#32-迁移原因)
  - [4. 🔍 迁移前分析](#4--迁移前分析)
    - [4.1. 数据库结构分析](#41-数据库结构分析)
    - [4.2. 兼容性评估](#42-兼容性评估)
    - [4.3. 数据类型分析](#43-数据类型分析)
  - [5. 🚀 迁移执行](#5--迁移执行)
    - [5.1. 迁移准备](#51-迁移准备)
  - [6. 表结构迁移](#6-表结构迁移)
    - [6.1. 数据迁移](#61-数据迁移)
    - [6.2. 索引和约束迁移](#62-索引和约束迁移)
  - [7. ⚠️ 遇到的问题和解决方案](#7-️-遇到的问题和解决方案)
    - [7.1. 问题1：时间戳转换](#71-问题1时间戳转换)
  - [8. 问题2：价格精度](#8-问题2价格精度)
    - [8.1. 问题3：外键约束](#81-问题3外键约束)
    - [8.2. 问题4：并发写入](#82-问题4并发写入)
  - [9. 📊 迁移后优化](#9--迁移后优化)
    - [9.1. 查询优化](#91-查询优化)
    - [9.2. 连接池优化](#92-连接池优化)
    - [9.3. 分区表](#93-分区表)
  - [10. 📈 性能对比](#10--性能对比)
  - [11. 💡 经验总结](#11--经验总结)
    - [11.1. 迁移前准备](#111-迁移前准备)
    - [11.2. 迁移执行](#112-迁移执行)
    - [11.3. 迁移后优化](#113-迁移后优化)
    - [11.4. 风险控制](#114-风险控制)
  - [12. 🔗 相关资源](#12--相关资源)
  - [13. 📚 参考资料](#13--参考资料)

---

## 2. 📋 案例概述

某电商系统从SQLite迁移到PostgreSQL的完整实践案例，涵盖迁移决策、执行过程、遇到的问题和解决方案。

---

## 3. 🎯 业务背景

### 3.1. 系统现状

- **数据库**：SQLite 3.35
- **数据量**：50GB
- **表数量**：25个表
- **用户量**：10万活跃用户
- **日订单量**：5000-10000单

### 3.2. 迁移原因

- **并发需求**：需要支持更高的并发写入
- **扩展性**：需要支持水平扩展
- **功能需求**：需要PostgreSQL的高级特性（窗口函数、JSONB等）
- **运维需求**：需要更好的监控和管理工具

---

## 4. 🔍 迁移前分析

### 4.1. 数据库结构分析

**主要表结构**：

```sql
-- 用户表
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    created_at INTEGER DEFAULT (strftime('%s', 'now'))
);

-- 商品表
CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    price REAL NOT NULL,
    stock INTEGER NOT NULL,
    category TEXT
);

-- 订单表
CREATE TABLE orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    total REAL NOT NULL,
    status TEXT NOT NULL DEFAULT 'pending',
    created_at INTEGER DEFAULT (strftime('%s', 'now')),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 订单项表
CREATE TABLE order_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id INTEGER NOT NULL,
    product_id INTEGER NOT NULL,
    quantity INTEGER NOT NULL,
    price REAL NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);
```

### 4.2. 兼容性评估

使用迁移前检查工具分析：

```bash
python tools/migration/01-迁移前检查.py database.db
```

**评估结果**：

- **兼容性评分**：85/100
- **主要风险**：
  - INTEGER类型范围检查（部分ID可能超出INTEGER范围）
  - REAL精度问题（价格需要NUMERIC）
  - TEXT长度限制（部分商品描述较长）

### 4.3. 数据类型分析

使用数据类型分析工具：

```bash
python tools/migration/02-数据类型分析.py database.db
```

**分析结果**：

- **price列**：REAL类型，建议映射为NUMERIC(10,2)
- **created_at列**：INTEGER时间戳，建议映射为TIMESTAMP
- **description列**：TEXT类型，部分超过255字符，建议使用TEXT类型

---

## 5. 🚀 迁移执行

### 5.1. 迁移准备

**步骤1：环境准备**:

- 搭建PostgreSQL 14环境
- 配置连接池（PgBouncer）
- 准备监控工具

**步骤2：数据备份**:

```bash
# SQLite备份
sqlite3 database.db ".backup backup.db"

# 验证备份
sqlite3 backup.db "SELECT COUNT(*) FROM users"
```

**步骤3：生成迁移脚本**:

```bash
python tools/migration/03-迁移脚本生成器.py database.db --output migration.sql
```

**完整迁移工具代码示例**：

```python
import sqlite3
import psycopg2
import os
from datetime import datetime

class SQLiteToPostgreSQLMigrator:
    """SQLite到PostgreSQL完整迁移工具"""

    def __init__(self, sqlite_path, pg_config):
        self.sqlite_path = sqlite_path
        self.pg_config = pg_config
        self.sqlite_conn = sqlite3.connect(sqlite_path)
        self.pg_conn = psycopg2.connect(**pg_config)

    def backup_sqlite(self, backup_path):
        """备份SQLite数据库"""
        print(f"备份SQLite数据库到: {backup_path}")
        os.system(f'sqlite3 {self.sqlite_path} ".backup {backup_path}"')
        print("✅ 备份完成")

    def convert_ddl(self, sqlite_ddl):
        """转换SQLite DDL到PostgreSQL"""
        ddl = sqlite_ddl
# AUTOINCREMENT -> SERIAL
        ddl = ddl.replace('INTEGER PRIMARY KEY AUTOINCREMENT', 'SERIAL PRIMARY KEY')
        ddl = ddl.replace('AUTOINCREMENT', 'SERIAL')
# 时间戳转换
        ddl = ddl.replace("DEFAULT (strftime('%s', 'now'))",
                         "DEFAULT EXTRACT(EPOCH FROM NOW())")
# REAL -> NUMERIC
        ddl = ddl.replace('REAL', 'DOUBLE PRECISION')
        return ddl

    def migrate_table(self, table_name, batch_size=10000):
        """迁移单个表"""
        print(f"\n迁移表: {table_name}")

# 1. 迁移表结构
        sqlite_cursor = self.sqlite_conn.cursor()
        sqlite_cursor.execute(f"SELECT sql FROM sqlite_master WHERE type='table' AND name='{table_name}'")
        sqlite_ddl = sqlite_cursor.fetchone()[0]
        pg_ddl = self.convert_ddl(sqlite_ddl)

        pg_cursor = self.pg_conn.cursor()
        pg_cursor.execute(pg_ddl)
        self.pg_conn.commit()
        print(f"  ✅ 表结构迁移完成")

# 2. 迁移数据
        sqlite_cursor.execute(f"PRAGMA table_info({table_name})")
        columns = [col[1] for col in sqlite_cursor.fetchall()]
        columns_str = ', '.join(columns)
        placeholders = ', '.join(['%s'] * len(columns))

        sqlite_cursor.execute(f"SELECT COUNT(*) FROM {table_name}")
        total_rows = sqlite_cursor.fetchone()[0]

        offset = 0
        while offset < total_rows:
            sqlite_cursor.execute(f"SELECT {columns_str} FROM {table_name} LIMIT ? OFFSET ?",
                                (batch_size, offset))
            rows = sqlite_cursor.fetchall()

            if rows:
                pg_cursor.executemany(
                    f"INSERT INTO {table_name} ({columns_str}) VALUES ({placeholders})",
                    rows
                )
                self.pg_conn.commit()

            offset += batch_size
            print(f"  已迁移 {min(offset, total_rows)}/{total_rows} 行")

        print(f"  ✅ 数据迁移完成")

# 3. 验证数据
        pg_cursor.execute(f"SELECT COUNT(*) FROM {table_name}")
        pg_count = pg_cursor.fetchone()[0]
        if total_rows == pg_count:
            print(f"  ✅ 数据验证通过: {total_rows} 行")
        else:
            print(f"  ❌ 数据不一致: SQLite={total_rows}, PostgreSQL={pg_count}")

    def migrate_all(self):
        """迁移所有表"""
# 获取所有表
        cursor = self.sqlite_conn.cursor()
        cursor.execute("SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'")
        tables = [row[0] for row in cursor.fetchall()]

        print(f"发现 {len(tables)} 个表需要迁移")

        for table_name in tables:
            self.migrate_table(table_name)

        print("\n✅ 所有表迁移完成")

# 使用示例
migrator = SQLiteToPostgreSQLMigrator(
    sqlite_path='ecommerce.db',
    pg_config={
        'host': 'localhost',
        'database': 'ecommerce',
        'user': 'postgres',
        'password': 'password'
    }
)
migrator.backup_sqlite('ecommerce.backup.db')
migrator.migrate_all()
```

## 6. 表结构迁移

**PostgreSQL表结构**：

```sql
-- 用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 商品表
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    price NUMERIC(10,2) NOT NULL,
    stock INTEGER NOT NULL,
    category VARCHAR(100)
);

-- 订单表
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    total NUMERIC(10,2) NOT NULL CHECK (total > 0),
    status VARCHAR(50) NOT NULL DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 订单项表
CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
    product_id INTEGER NOT NULL REFERENCES products(id),
    quantity INTEGER NOT NULL CHECK (quantity > 0),
    price NUMERIC(10,2) NOT NULL CHECK (price > 0)
);
```

### 6.1. 数据迁移

**使用数据迁移工具**：

```bash
python tools/migration/04-数据迁移工具.py database.db \
    postgresql://user:pass@host/db \
    --batch-size 5000 \
    --verify
```

**迁移统计**：

- **总行数**：1,200万行
- **迁移时间**：2小时15分钟
- **平均速度**：1,500行/秒
- **错误数**：0

### 6.2. 索引和约束迁移

**索引迁移**：

```sql
-- 用户表索引
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_created_at ON users(created_at DESC);

-- 订单表索引
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_created_at ON orders(created_at DESC);

-- 订单项表索引
CREATE INDEX idx_order_items_order_id ON order_items(order_id);
CREATE INDEX idx_order_items_product_id ON order_items(product_id);
```

---

## 7. ⚠️ 遇到的问题和解决方案

### 7.1. 问题1：时间戳转换

**问题**：SQLite使用INTEGER存储Unix时间戳，PostgreSQL使用TIMESTAMP

**解决方案**：

```python
# 迁移脚本中的转换
def convert_timestamp(sqlite_timestamp):
    if sqlite_timestamp:
        return datetime.fromtimestamp(sqlite_timestamp)
    return None
```

## 8. 问题2：价格精度

**问题**：REAL类型精度问题，导致价格计算误差

**解决方案**：

- 迁移时使用NUMERIC(10,2)
- 数据验证确保精度正确

### 8.1. 问题3：外键约束

**问题**：SQLite外键默认关闭，PostgreSQL强制检查

**解决方案**：

- 迁移前验证所有外键数据有效性
- 修复无效的外键引用

### 8.2. 问题4：并发写入

**问题**：迁移过程中系统仍在运行，有新数据写入

**解决方案**：

- 使用双写策略
- 迁移完成后同步增量数据

---

## 9. 📊 迁移后优化

### 9.1. 查询优化

**优化前（SQLite）**：

```sql
SELECT * FROM orders WHERE user_id = ? AND status = 'pending';
```

**优化后（PostgreSQL）**：

```sql
-- 使用覆盖索引
CREATE INDEX idx_orders_user_status_covering
ON orders(user_id, status) INCLUDE (total, created_at);

SELECT total, created_at FROM orders
WHERE user_id = ? AND status = 'pending';
```

### 9.2. 连接池优化

**配置PgBouncer**：

```ini
[databases]
ecommerce = host=localhost port=5432 dbname=ecommerce

[pgbouncer]
pool_mode = transaction
max_client_conn = 1000
default_pool_size = 25
```

### 9.3. 分区表

**按时间分区订单表**：

```sql
-- 按月分区
CREATE TABLE orders_2024_01 PARTITION OF orders
FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
```

---

## 10. 📈 性能对比

| 指标 | SQLite | PostgreSQL | 提升 |
|------|--------|-----------|------|
| **查询性能** | 10ms | 3ms | 3.3x |
| **并发写入** | 50 TPS | 500 TPS | 10x |
| **复杂查询** | 不支持 | 支持 | - |
| **数据量上限** | 140TB | 无限制 | - |

---

## 11. 💡 经验总结

### 11.1. 迁移前准备

- ✅ 充分的数据分析
- ✅ 完整的备份策略
- ✅ 详细的迁移计划

### 11.2. 迁移执行

- ✅ 使用自动化工具
- ✅ 分阶段执行
- ✅ 实时监控

### 11.3. 迁移后优化

- ✅ 索引优化
- ✅ 查询重写
- ✅ 连接池配置

### 11.4. 风险控制

- ✅ 回滚方案准备
- ✅ 数据验证
- ✅ 灰度发布

---

## 12. 🔗 相关资源

- [10.01 SQLite到PostgreSQL迁移指南](../10-迁移指南/10.01-SQLite到PostgreSQL迁移指南.md)
- [10.02 SQL兼容性深度分析](../10-迁移指南/10.02-SQL兼容性深度分析.md)
- [迁移工具](../tools/migration/)
- [迁移示例](../examples/03-migration/)

---

## 13. 📚 参考资料

- [PostgreSQL官方文档](https://www.postgresql.org/docs/)
- [pgloader文档](https://pgloader.readthedocs.io/)

---

**维护者**：Data-Science Team
**最后更新**：2025-01-15

# SQL概念本体 (SQL Ontology)

> **创建日期**：2025-12-01
> **最后更新**：2025-12-01
> **版本**：v1.0.0
> **目的**：建立SQL知识体系的完整概念本体，定义概念层次、关系和约束

---

## 📋 目录

- [SQL概念本体 (SQL Ontology)](#sql概念本体-sql-ontology)
  - [📋 目录](#-目录)
  - [一、本体概述](#一本体概述)
    - [1.1 本体定义](#11-本体定义)
    - [1.2 设计原则](#12-设计原则)
  - [二、顶层概念分类](#二顶层概念分类)
  - [三、概念层次结构](#三概念层次结构)
    - [3.1 数学基础层 (MathFoundation)](#31-数学基础层-mathfoundation)
    - [3.2 数据模型层 (DataModel)](#32-数据模型层-datamodel)
    - [3.3 查询语言层 (QueryLanguage)](#33-查询语言层-querylanguage)
    - [3.4 执行系统层 (ExecutionSystem)](#34-执行系统层-executionsystem)
    - [3.5 约束系统层 (ConstraintSystem)](#35-约束系统层-constraintsystem)
  - [四、概念关系定义](#四概念关系定义)
    - [4.1 关系类型清单](#41-关系类型清单)
    - [4.2 核心关系图](#42-核心关系图)
  - [五、概念属性定义](#五概念属性定义)
    - [5.1 核心概念属性](#51-核心概念属性)
  - [六、概念约束规则](#六概念约束规则)
    - [6.1 层次约束](#61-层次约束)
    - [6.2 操作约束](#62-操作约束)
  - [七、概念索引](#七概念索引)
    - [7.1 按字母顺序](#71-按字母顺序)
    - [7.2 按依赖顺序](#72-按依赖顺序)
  - [相关资源](#相关资源)
    - [内部链接](#内部链接)

---

## 一、本体概述

### 1.1 本体定义

```text
SQL概念本体是SQL知识体系的形式化表示，包含：
- 概念(Classes)：SQL领域的核心实体类型
- 关系(Relations)：概念之间的语义联系
- 属性(Properties)：概念的特征描述
- 约束(Constraints)：概念和关系的限制规则
- 实例(Instances)：具体的概念实例
```

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| **完整性** | 覆盖SQL所有核心概念 |
| **一致性** | 概念定义无矛盾 |
| **层次性** | 清晰的继承结构 |
| **可扩展性** | 支持新概念添加 |

---

## 二、顶层概念分类

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                        SQL概念本体顶层结构                               │
└─────────────────────────────────────────────────────────────────────────┘

                           SQLConcept
                               │
        ┌──────────┬──────────┼──────────┬──────────┐
        │          │          │          │          │
        ▼          ▼          ▼          ▼          ▼
   ┌─────────┐┌─────────┐┌─────────┐┌─────────┐┌─────────┐
   │数学基础 ││数据模型 ││查询语言 ││执行系统 ││约束系统 │
   │  Math   ││DataModel││QueryLang││Execution││Constrai │
   └────┬────┘└────┬────┘└────┬────┘└────┬────┘└────┬────┘
        │          │          │          │          │
   集合/函数   关系/模式   SQL语法    算子/计划    完整性
   /谓词      /元组       /代数      /优化       /事务

═══════════════════════════════════════════════════════════════════════════
```

---

## 三、概念层次结构

### 3.1 数学基础层 (MathFoundation)

```text
MathFoundation
├── SetTheory [集合论]
│   ├── Set [集合]
│   │   ├── EmptySet [空集] is-a Set
│   │   ├── FiniteSet [有限集] is-a Set
│   │   └── PowerSet [幂集] is-a Set
│   ├── CartesianProduct [笛卡尔积]
│   └── Subset [子集关系]
│
├── FunctionTheory [函数论]
│   ├── Function [函数]
│   │   ├── Injection [单射] is-a Function
│   │   ├── Surjection [满射] is-a Function
│   │   └── Bijection [双射] is-a Function
│   └── Mapping [映射]
│
├── PredicateLogic [谓词逻辑]
│   ├── Predicate [谓词]
│   ├── Quantifier [量词]
│   │   ├── Universal [全称量词] is-a Quantifier
│   │   └── Existential [存在量词] is-a Quantifier
│   └── Formula [公式]
│
└── Algebra [代数]
    ├── AlgebraicStructure [代数结构]
    └── Operation [操作]
```

### 3.2 数据模型层 (DataModel)

```text
DataModel
├── RelationalModel [关系模型]
│   ├── Domain [域]
│   │   └── AtomicDomain [原子域] is-a Domain
│   │
│   ├── Relation [关系]
│   │   has-a Tuple (0..*)
│   │   has-a Attribute (1..*)
│   │   implements Set
│   │
│   ├── Tuple [元组]
│   │   part-of Relation
│   │   has-a AttributeValue (1..*)
│   │
│   ├── Attribute [属性]
│   │   part-of Relation
│   │   has-a Domain (1)
│   │
│   ├── RelationSchema [关系模式]
│   │   defines Relation
│   │   has-a Attribute (1..*)
│   │   has-a Constraint (0..*)
│   │
│   └── RelationalDatabase [关系数据库]
│       has-a RelationSchema (1..*)
│
├── FunctionalDependency [函数依赖]
│   ├── FullFD [完全函数依赖] is-a FunctionalDependency
│   ├── PartialFD [部分函数依赖] is-a FunctionalDependency
│   └── TransitiveFD [传递函数依赖] is-a FunctionalDependency
│
├── NormalForm [范式]
│   ├── 1NF is-a NormalForm
│   ├── 2NF is-a NormalForm, stronger-than 1NF
│   ├── 3NF is-a NormalForm, stronger-than 2NF
│   ├── BCNF is-a NormalForm, stronger-than 3NF
│   ├── 4NF is-a NormalForm, stronger-than BCNF
│   └── 5NF is-a NormalForm, stronger-than 4NF
│
└── Key [键]
    ├── SuperKey [超键] is-a Key
    ├── CandidateKey [候选键] is-a SuperKey (minimal)
    ├── PrimaryKey [主键] is-a CandidateKey
    └── ForeignKey [外键] is-a Key, references PrimaryKey
```

### 3.3 查询语言层 (QueryLanguage)

```text
QueryLanguage
├── RelationalAlgebra [关系代数]
│   ├── UnaryOperation [一元操作]
│   │   ├── Selection (σ) [选择]
│   │   ├── Projection (π) [投影]
│   │   └── Rename (ρ) [重命名]
│   │
│   ├── BinaryOperation [二元操作]
│   │   ├── Union (∪) [并]
│   │   ├── Intersection (∩) [交]
│   │   ├── Difference (-) [差]
│   │   ├── CartesianProduct (×) [笛卡尔积]
│   │   ├── Join (⋈) [连接]
│   │   │   ├── NaturalJoin is-a Join
│   │   │   ├── ThetaJoin is-a Join
│   │   │   ├── EquiJoin is-a ThetaJoin
│   │   │   ├── LeftOuterJoin is-a Join
│   │   │   ├── RightOuterJoin is-a Join
│   │   │   └── FullOuterJoin is-a Join
│   │   └── Division (÷) [除]
│   │
│   └── ExtendedOperation [扩展操作]
│       ├── Aggregation (γ) [聚合]
│       ├── Sorting (τ) [排序]
│       └── Grouping [分组]
│
├── RelationalCalculus [关系演算]
│   ├── TupleCalculus [元组演算]
│   └── DomainCalculus [域演算]
│
└── SQL [结构化查询语言]
    ├── DDL [数据定义语言]
    │   ├── CREATE
    │   │   ├── CREATE_TABLE
    │   │   ├── CREATE_INDEX
    │   │   └── CREATE_VIEW
    │   ├── ALTER
    │   └── DROP
    │
    ├── DML [数据操作语言]
    │   ├── INSERT
    │   ├── UPDATE
    │   └── DELETE
    │
    ├── DQL [数据查询语言]
    │   └── SELECT
    │       ├── SELECT_clause implements Projection
    │       ├── FROM_clause
    │       ├── WHERE_clause implements Selection
    │       ├── JOIN_clause implements Join
    │       ├── GROUP_BY_clause implements Grouping
    │       ├── HAVING_clause
    │       ├── ORDER_BY_clause implements Sorting
    │       └── LIMIT_clause
    │
    ├── DCL [数据控制语言]
    │   ├── GRANT
    │   └── REVOKE
    │
    └── TCL [事务控制语言]
        ├── BEGIN
        ├── COMMIT
        ├── ROLLBACK
        └── SAVEPOINT
```

### 3.4 执行系统层 (ExecutionSystem)

```text
ExecutionSystem
├── QueryProcessor [查询处理器]
│   ├── Parser [解析器]
│   │   produces AST
│   ├── Analyzer [分析器]
│   │   produces LogicalPlan
│   ├── Optimizer [优化器]
│   │   ├── LogicalOptimizer
│   │   │   applies TransformationRule
│   │   └── PhysicalOptimizer
│   │       uses CostModel
│   └── Executor [执行器]
│       executes PhysicalPlan
│
├── ExecutionPlan [执行计划]
│   ├── LogicalPlan [逻辑计划]
│   │   consists-of LogicalOperator
│   └── PhysicalPlan [物理计划]
│       consists-of PhysicalOperator
│
├── Operator [算子]
│   ├── LogicalOperator [逻辑算子]
│   │   ├── LogicalScan
│   │   ├── LogicalFilter
│   │   ├── LogicalProject
│   │   ├── LogicalJoin
│   │   └── LogicalAggregate
│   │
│   └── PhysicalOperator [物理算子]
│       ├── TableScan
│       │   ├── SeqScan
│       │   ├── IndexScan
│       │   └── BitmapScan
│       ├── Filter
│       ├── Project
│       ├── JoinOperator
│       │   ├── NestedLoopJoin
│       │   ├── HashJoin
│       │   └── MergeJoin
│       ├── Sort
│       ├── Aggregate
│       │   ├── HashAggregate
│       │   └── GroupAggregate
│       └── Limit
│
└── CostModel [代价模型]
    has-a IOCost
    has-a CPUCost
    has-a MemoryCost
    uses Statistics
```

### 3.5 约束系统层 (ConstraintSystem)

```text
ConstraintSystem
├── IntegrityConstraint [完整性约束]
│   ├── EntityIntegrity [实体完整性]
│   │   enforces PrimaryKey NOT NULL
│   ├── ReferentialIntegrity [参照完整性]
│   │   enforces ForeignKey references
│   └── UserDefinedIntegrity [用户定义完整性]
│       ├── DomainConstraint [域约束]
│       ├── CheckConstraint [检查约束]
│       ├── UniqueConstraint [唯一约束]
│       └── NotNullConstraint [非空约束]
│
├── TransactionProperty [事务属性]
│   ├── Atomicity [原子性]
│   ├── Consistency [一致性]
│   ├── Isolation [隔离性]
│   │   ├── ReadUncommitted is-a IsolationLevel
│   │   ├── ReadCommitted is-a IsolationLevel
│   │   ├── RepeatableRead is-a IsolationLevel
│   │   └── Serializable is-a IsolationLevel
│   └── Durability [持久性]
│
└── ConcurrencyControl [并发控制]
    ├── Lock [锁]
    │   ├── SharedLock is-a Lock
    │   └── ExclusiveLock is-a Lock
    ├── MVCC [多版本并发控制]
    └── TwoPhaseCommit [两阶段提交]
```

---

## 四、概念关系定义

### 4.1 关系类型清单

```text
┌────────────────────────────────────────────────────────────────────────┐
│                         概念关系类型定义                                │
├──────────────┬─────────────────────────────────────────────────────────┤
│ 关系类型      │ 定义与示例                                              │
├──────────────┼─────────────────────────────────────────────────────────┤
│ is-a         │ 泛化/特化关系                                           │
│              │ 例: CandidateKey is-a SuperKey                         │
├──────────────┼─────────────────────────────────────────────────────────┤
│ part-of      │ 组成关系（部分→整体）                                    │
│              │ 例: Tuple part-of Relation                              │
├──────────────┼─────────────────────────────────────────────────────────┤
│ has-a        │ 包含关系（整体→部分）                                    │
│              │ 例: Relation has-a Attribute                           │
├──────────────┼─────────────────────────────────────────────────────────┤
│ implements   │ 实现关系（具体→抽象）                                    │
│              │ 例: SELECT_clause implements Projection                 │
├──────────────┼─────────────────────────────────────────────────────────┤
│ abstracts    │ 抽象关系（抽象→具体）                                    │
│              │ 例: RelationalAlgebra abstracts SQL                     │
├──────────────┼─────────────────────────────────────────────────────────┤
│ depends-on   │ 依赖关系                                                │
│              │ 例: 3NF depends-on FunctionalDependency                 │
├──────────────┼─────────────────────────────────────────────────────────┤
│ produces     │ 产出关系                                                │
│              │ 例: Parser produces AST                                 │
├──────────────┼─────────────────────────────────────────────────────────┤
│ uses         │ 使用关系                                                │
│              │ 例: PhysicalOptimizer uses CostModel                    │
├──────────────┼─────────────────────────────────────────────────────────┤
│ references   │ 引用关系                                                │
│              │ 例: ForeignKey references PrimaryKey                    │
├──────────────┼─────────────────────────────────────────────────────────┤
│ stronger-than│ 强度关系（范式层次）                                     │
│              │ 例: 3NF stronger-than 2NF                               │
├──────────────┼─────────────────────────────────────────────────────────┤
│ equivalent-to│ 等价关系                                                │
│              │ 例: RelationalAlgebra equivalent-to TupleCalculus       │
├──────────────┼─────────────────────────────────────────────────────────┤
│ causes       │ 因果关系                                                │
│              │ 例: PartialFD causes DataRedundancy                     │
├──────────────┼─────────────────────────────────────────────────────────┤
│ prevents     │ 阻止关系                                                │
│              │ 例: 2NF prevents PartialFD                              │
└──────────────┴─────────────────────────────────────────────────────────┘
```

### 4.2 核心关系图

```text
═══════════════════════════════════════════════════════════════════════════
                        SQL概念核心关系图
═══════════════════════════════════════════════════════════════════════════

  SetTheory ──────────────────────────────────────────────────────────┐
      │                                                               │
      │ defines                                                       │
      ▼                                                               │
  CartesianProduct ──[subset-of]──→ Relation ←──[implements]── Table  │
                                       │                              │
                 ┌─────────────────────┼─────────────────────┐       │
                 │                     │                     │        │
                 ▼                     ▼                     ▼        │
             has-a                 has-a               part-of        │
                 │                     │                     │        │
                 ▼                     ▼                     ▼        │
            Attribute              Tuple          RelationSchema       │
                 │                                       │             │
                 └───────────────────┬───────────────────┘             │
                                     │                                  │
                            defines constraints                         │
                                     │                                  │
                                     ▼                                  │
                           FunctionalDependency ──────────────┐        │
                                     │                         │        │
                        ┌────────────┼────────────┐            │        │
                        │            │            │            │        │
                        ▼            ▼            ▼            │        │
                    FullFD     PartialFD   TransitiveFD       │        │
                        │            │            │            │        │
                        │    causes  │    causes  │            │        │
                        ▼            ▼            ▼            │        │
                    ┌────────────────────────────────┐         │        │
                    │      DataRedundancy            │         │        │
                    │      UpdateAnomaly             │         │        │
                    └────────────────────────────────┘         │        │
                                     │                         │        │
                        prevented-by │                         │        │
                                     ▼                         │        │
                    ┌────────────────────────────────┐         │        │
                    │         NormalForm             │←────────┘        │
                    │  1NF < 2NF < 3NF < BCNF < 4NF │                   │
                    └────────────────────────────────┘                   │
                                                                         │
  RelationalAlgebra ←───────────[abstracts]──────────────────────────────┘
      │
      │ equivalent-to
      ▼
  TupleCalculus
      │
      │ implements (SQL)
      ▼
  SQL ──┬── DDL ──→ CREATE/ALTER/DROP
        │
        ├── DML ──→ INSERT/UPDATE/DELETE
        │
        ├── DQL ──→ SELECT ──┬── WHERE ←─[implements]── Selection(σ)
        │                    ├── SELECT列 ←─[implements]── Projection(π)
        │                    └── JOIN ←─[implements]── Join(⋈)
        │
        └── TCL ──→ Transaction ──→ ACID
                         │
                         └── IsolationLevel
═══════════════════════════════════════════════════════════════════════════
```

---

## 五、概念属性定义

### 5.1 核心概念属性

```text
Relation (关系)
─────────────────────────────────────────────────
属性:
- degree: Integer [1..*]      // 属性数量
- cardinality: Integer [0..*] // 元组数量
- schema: RelationSchema [1]  // 关系模式

约束:
- degree >= 1
- cardinality >= 0

Attribute (属性)
─────────────────────────────────────────────────
属性:
- name: String               // 属性名
- domain: Domain [1]         // 所属域
- nullable: Boolean          // 是否可空
- defaultValue: Any?         // 默认值

FunctionalDependency (函数依赖)
─────────────────────────────────────────────────
属性:
- determinant: Set<Attribute> [1..*]  // 决定因素
- dependent: Set<Attribute> [1..*]    // 依赖属性
- type: FDType {Full, Partial, Transitive}

NormalForm (范式)
─────────────────────────────────────────────────
属性:
- level: Integer [1..6]       // 范式级别
- constraint: String          // 满足条件
- prevents: Set<Anomaly>      // 防止的异常

Operator (算子)
─────────────────────────────────────────────────
属性:
- inputArity: Integer [1..2]  // 输入关系数
- outputSchema: Schema        // 输出模式
- cost: CostEstimate          // 代价估计
- cardinality: Integer        // 结果基数估计
```

---

## 六、概念约束规则

### 6.1 层次约束

```text
约束C1: 范式层次单调性
─────────────────────
∀ NF1, NF2: stronger-than(NF1, NF2) → satisfies(R, NF1) → satisfies(R, NF2)

约束C2: 键的最小性
────────────────────
∀ CK ∈ CandidateKeys(R):
  ¬∃ K' ⊂ CK: isKey(K', R)

约束C3: 外键引用完整性
────────────────────
∀ FK referencing PK:
  ∀ t ∈ R: t[FK] = NULL ∨ ∃ s ∈ S: t[FK] = s[PK]
```

### 6.2 操作约束

```text
约束O1: 关系代数类型安全
───────────────────────
Union(R, S): schema(R) = schema(S)
Intersection(R, S): schema(R) = schema(S)
Difference(R, S): schema(R) = schema(S)

约束O2: 投影属性存在性
────────────────────
π_X(R): X ⊆ attrs(R)

约束O3: 选择谓词类型
──────────────────
σ_p(R): attrs(p) ⊆ attrs(R)
```

---

## 七、概念索引

### 7.1 按字母顺序

| ID | 概念 | 英文 | 所属层 | 类型 |
|----|------|------|--------|------|
| A001 | 聚合 | Aggregation | QueryLanguage | Operation |
| A002 | 属性 | Attribute | DataModel | Class |
| B001 | BCNF | BCNF | DataModel | NormalForm |
| C001 | 候选键 | CandidateKey | DataModel | Key |
| C002 | 笛卡尔积 | CartesianProduct | Math | Operation |
| D001 | 域 | Domain | DataModel | Class |
| F001 | 函数依赖 | FunctionalDependency | DataModel | Class |
| J001 | 连接 | Join | QueryLanguage | Operation |
| N001 | 范式 | NormalForm | DataModel | Class |
| P001 | 投影 | Projection | QueryLanguage | Operation |
| R001 | 关系 | Relation | DataModel | Class |
| S001 | 选择 | Selection | QueryLanguage | Operation |
| T001 | 元组 | Tuple | DataModel | Class |

### 7.2 按依赖顺序

```text
学习路径（按依赖排序）：

Level 0: 无依赖
├── Set, Function, Predicate

Level 1: 依赖Level 0
├── CartesianProduct (depends-on Set)
├── Domain (depends-on Set)

Level 2: 依赖Level 1
├── Relation (depends-on CartesianProduct)
├── Attribute (depends-on Domain)
├── Tuple (depends-on Relation)

Level 3: 依赖Level 2
├── FunctionalDependency (depends-on Attribute)
├── Key (depends-on Attribute)
├── RelationalAlgebra (depends-on Relation)

Level 4: 依赖Level 3
├── NormalForm (depends-on FunctionalDependency)
├── SQL (depends-on RelationalAlgebra)

Level 5: 依赖Level 4
├── QueryOptimization (depends-on SQL)
├── Transaction (depends-on SQL)
```

---

## 相关资源

### 内部链接

- [统一概念定义框架](./09.05-统一概念定义框架.md)
- [SQL知识体系关系图谱](./09.04-SQL知识体系关系图谱.md)
- [数学基础到SQL因果链](../01-理论基础/01.06-数学基础到SQL因果链.md)

---

**维护者**: SQL Standards Team
**创建日期**: 2025-12-01
**概念数量**: 100+
